//#byte   TXEN = 0xAC                          //transmit enable register

#define PIN_TX    PIN_E2                     //transmit PIN
#define PIN_RX    PIN_E3                     //receive PIN

//#define PIN_TX    PIN_C4                     //transmit PIN
//#define PIN_RX    PIN_C5                     //receive PIN

///////////////////////////////////////////////////////////////////////////////
// SDI-12 standard baudrate=1200, parity=even, bit=7, stop bit=1, stream=SDI12
///////////////////////////////////////////////////////////////////////////////
//#use rs232(force_sw,invert,baud=1200,BRGH1OK, parity=e, bits=7, stop=1,xmit=PIN_TX,rcv=PIN_RX, stream=SDI12)
#use rs232(invert,baud=1200,BRGH1OK, parity=e, bits=7, stop=1,xmit=PIN_TX,rcv=PIN_RX, stream=SDI12)
//#use rs232(baud=12200,BRGH1OK, parity=n, bits=8, stop=1,xmit=PIN_TX,rcv=PIN_RX, stream=Flas

void sdi12_start();
void sdi12_end();
void sdi12_receive(int1 cpx);
void sdi12_startdata(char c);
void sdi12_address();
void sdi12_getData(char c);
void sdi12_probeId(char c);
int1 sdi12_process(char *cmdx);
void getSoilmoistureData(char add);
