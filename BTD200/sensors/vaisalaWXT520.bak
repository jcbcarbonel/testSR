#use rs232(baud=9600, xmit=PIN_G4, rcv=PIN_G3, stream=SNSR_AIO) //aio
#use rs232(baud=1200, xmit=PIN_G4, rcv=PIN_G3, parity=E, bits=7, stream=SNSR_VAI) //vaisala

/*
   VAISALA LIBRARY
   
   - int Get_Data_Vai(char *strx)
      * returns stream from vaisala sensor
      * data is stored in VAISALA_STREAM variable
      
   - void vaisala_Rain_Cum()
      * returns cummulative rain from vaisala sensor
      * data is stored in VAISALA_RAIN_CUM
*/
#define __VAISALA_LIB

char VAISALA_STREAM[90]={};
float VAISALA_RAIN_CUM=0;

int Get_Data_AIO(char *strx);
int Get_Data_Vai(char *strx);

